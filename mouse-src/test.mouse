
fn fibonacci(n) {
    if n <= 1 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

print(fibonacci(10));
let fib = fibonacci(11);

let count = 0;
while count < 10 {
    print("FIBONACCI: " + fibonacci(count));
    count = count + 1;
}
