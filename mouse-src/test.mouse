
fn fibonacci(n) {
    if n <= 1 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

// print(fibonacci(10));
let fib = fibonacci(10);
let fib_str = fib;
print("FIB " + fib_str);
