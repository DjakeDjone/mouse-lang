fn add(a, b) {
    return a + b;
}

fn multiply(a, b) {
    return a * b;
}

let x = 10;
let y = 20;

# Test basic arithmetic with proper precedence;
let result1 = 5 + 3 * 2;
let result2 = 10 / 2 + 3;
let result3 = 2 * 3 + 4 * 5;
let result4 = 100 - 50 / 5;

# Test function calls in expressions;
let result5 = add(1, 2) + add(3, 4);
let result6 = multiply(2, 3) + multiply(4, 5);
let result7 = add(1, 2) * multiply(3, 4);

# Test nested expressions;
let result8 = 1 + 2 * 3 / 6 - 4;
let result9 = add(multiply(2, 3), add(4, 5));

# Test comparisons;
let cmp1 = 5 + 3 > 7;
let cmp2 = 10 / 2 == 5;
let cmp3 = 2 * 3 < 10;

# Test mixed function calls and arithmetic;
let result10 = add(5, 10) / 3 + multiply(2, 4);
